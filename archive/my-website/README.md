# Archived Docusaurus site

This folder was moved from `my-website/` at the repo root. It is a standalone Docusaurus project and is **not** part of the Next.js ProDoc portfolio app.

Run commands from this directory only if you need to maintain the legacy Docusaurus demo separately.
